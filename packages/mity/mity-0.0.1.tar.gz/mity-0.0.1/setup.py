#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['mity']

package_data = \
{'': ['*']}

install_requires = \
['invoke', 'knack']

extras_require = \
{'doc': ['sphinx'], 'test': ['pytest']}

entry_points = \
{'console_scripts': ['mity = mity.mity:main']}

setup(name='mity',
      version='0.0.1',
      description='Mity: invoke orthogonal workflows from a single tool',
      author='Hamza Sheikh',
      author_email='code@codeghar.com',
      url='https://aikchar.me/blog/what-is-mity.html',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='~=3.7',
     )
