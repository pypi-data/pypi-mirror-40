#!/usr/bin/env python
from gityamldb import GitYamlDB
import sys
import argparse

root_parser = argparse.ArgumentParser()
root_parser.add_argument('repo_path')

args = root_parser.parse_args()

db = GitYamlDB(args.repo_path)
if db.validate_all():
    exit(0)
else:
    sys.stderr.write('ERR\n')
    exit(1)
